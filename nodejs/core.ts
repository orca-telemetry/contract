// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v4.25.9
// source: core.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "./google/protobuf/timestamp";
import {
  ComputeMetrics,
  DataFunctionSettings,
  ExecutionStatus,
  executionStatusFromJSON,
  executionStatusToJSON,
  RegistrationStatus,
  registrationStatusFromJSON,
  registrationStatusToJSON,
  TaskExecutionSettings,
  TriggerSource,
  triggerSourceFromJSON,
  triggerSourceToJSON,
  TriggerStatus,
  triggerStatusFromJSON,
  triggerStatusToJSON,
  WorkflowExecutionSettings,
} from "./shared";

export const protobufPackage = "";

/** The comparator to apply in a LeafFilter. */
export enum Comparator {
  COMPARATOR_UNSPECIFIED = 0,
  /** EQ - Equal to */
  EQ = 1,
  /** NEQ - Not equal to */
  NEQ = 2,
  /** GT - Greater than */
  GT = 3,
  /** GTE - Greater than or equal to */
  GTE = 4,
  /** LT - Less than */
  LT = 5,
  /** LTE - Less than or equal to */
  LTE = 6,
  /** IN - Value is one of a set */
  IN = 7,
  /** NOT_IN - Value is not any of a set */
  NOT_IN = 8,
  /** EXISTS - Key is present (no value required) */
  EXISTS = 9,
  /** NOT_EXISTS - Key is absent (no value required) */
  NOT_EXISTS = 10,
  /** CONTAINS - Value contains the given substring */
  CONTAINS = 11,
  /** PREFIX - Value starts with the given string */
  PREFIX = 12,
  UNRECOGNIZED = -1,
}

export function comparatorFromJSON(object: any): Comparator {
  switch (object) {
    case 0:
    case "COMPARATOR_UNSPECIFIED":
      return Comparator.COMPARATOR_UNSPECIFIED;
    case 1:
    case "EQ":
      return Comparator.EQ;
    case 2:
    case "NEQ":
      return Comparator.NEQ;
    case 3:
    case "GT":
      return Comparator.GT;
    case 4:
    case "GTE":
      return Comparator.GTE;
    case 5:
    case "LT":
      return Comparator.LT;
    case 6:
    case "LTE":
      return Comparator.LTE;
    case 7:
    case "IN":
      return Comparator.IN;
    case 8:
    case "NOT_IN":
      return Comparator.NOT_IN;
    case 9:
    case "EXISTS":
      return Comparator.EXISTS;
    case 10:
    case "NOT_EXISTS":
      return Comparator.NOT_EXISTS;
    case 11:
    case "CONTAINS":
      return Comparator.CONTAINS;
    case 12:
    case "PREFIX":
      return Comparator.PREFIX;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Comparator.UNRECOGNIZED;
  }
}

export function comparatorToJSON(object: Comparator): string {
  switch (object) {
    case Comparator.COMPARATOR_UNSPECIFIED:
      return "COMPARATOR_UNSPECIFIED";
    case Comparator.EQ:
      return "EQ";
    case Comparator.NEQ:
      return "NEQ";
    case Comparator.GT:
      return "GT";
    case Comparator.GTE:
      return "GTE";
    case Comparator.LT:
      return "LT";
    case Comparator.LTE:
      return "LTE";
    case Comparator.IN:
      return "IN";
    case Comparator.NOT_IN:
      return "NOT_IN";
    case Comparator.EXISTS:
      return "EXISTS";
    case Comparator.NOT_EXISTS:
      return "NOT_EXISTS";
    case Comparator.CONTAINS:
      return "CONTAINS";
    case Comparator.PREFIX:
      return "PREFIX";
    case Comparator.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Identifies which data source a key belongs to, used in OrderByStatement. */
export enum DataSource {
  DATA_SOURCE_UNSPECIFIED = 0,
  RESULT = 1,
  EXECUTION_PARAMETERS = 2,
  UNRECOGNIZED = -1,
}

export function dataSourceFromJSON(object: any): DataSource {
  switch (object) {
    case 0:
    case "DATA_SOURCE_UNSPECIFIED":
      return DataSource.DATA_SOURCE_UNSPECIFIED;
    case 1:
    case "RESULT":
      return DataSource.RESULT;
    case 2:
    case "EXECUTION_PARAMETERS":
      return DataSource.EXECUTION_PARAMETERS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataSource.UNRECOGNIZED;
  }
}

export function dataSourceToJSON(object: DataSource): string {
  switch (object) {
    case DataSource.DATA_SOURCE_UNSPECIFIED:
      return "DATA_SOURCE_UNSPECIFIED";
    case DataSource.RESULT:
      return "RESULT";
    case DataSource.EXECUTION_PARAMETERS:
      return "EXECUTION_PARAMETERS";
    case DataSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Sort direction for an OrderByStatement. */
export enum SortDirection {
  SORT_DIRECTION_UNSPECIFIED = 0,
  ASC = 1,
  DESC = 2,
  UNRECOGNIZED = -1,
}

export function sortDirectionFromJSON(object: any): SortDirection {
  switch (object) {
    case 0:
    case "SORT_DIRECTION_UNSPECIFIED":
      return SortDirection.SORT_DIRECTION_UNSPECIFIED;
    case 1:
    case "ASC":
      return SortDirection.ASC;
    case 2:
    case "DESC":
      return SortDirection.DESC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SortDirection.UNRECOGNIZED;
  }
}

export function sortDirectionToJSON(object: SortDirection): string {
  switch (object) {
    case SortDirection.SORT_DIRECTION_UNSPECIFIED:
      return "SORT_DIRECTION_UNSPECIFIED";
    case SortDirection.ASC:
      return "ASC";
    case SortDirection.DESC:
      return "DESC";
    case SortDirection.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** DataFunction defines a registered data function and its metadata. */
export interface DataFunction {
  /** Name is the globally unique identifier for this data function. */
  name?:
    | string
    | undefined;
  /** Hash is the hash of the AST segment that defines the data function. */
  hash?:
    | string
    | undefined;
  /**
   * InputModel is a marshalled JSON schema describing the accepted input.
   * This model must be satisfied by the execution model of the owning workflow.
   */
  inputModel?:
    | string
    | undefined;
  /**
   * OutputModel is a marshalled JSON schema describing a single output record.
   * The data function produces an array of this schema, streamed to the
   * orchestrator for caching. Validated once at retrieval time.
   */
  outputModel?:
    | string
    | undefined;
  /**
   * Settings governs the lifecycle and retention of data produced by this
   * data function.
   */
  settings?: DataFunctionSettings | undefined;
}

/** Task defines a registered task and its execution configuration. */
export interface Task {
  /** TaskHash is the hash of the AST segment corresponding to this task. */
  taskHash?:
    | string
    | undefined;
  /** Name is the unique name of this task. */
  name?:
    | string
    | undefined;
  /** Description is a human-readable summary of the task's purpose. */
  description?:
    | string
    | undefined;
  /** ExecutionSettings governs execution timeouts, retries, backoff, and SLA deadlines. */
  executionSettings?:
    | TaskExecutionSettings
    | undefined;
  /**
   * InputModel is a marshalled JSON schema (JSON Schema Standard) describing
   * the task's accepted input. Validation and pointer extensions are not enforced.
   */
  inputModel?:
    | string
    | undefined;
  /**
   * OutputModel is a marshalled JSON schema (JSON Schema Standard) describing
   * the task's output. Validation and pointer extensions are not enforced.
   */
  outputModel?:
    | string
    | undefined;
  /** RequiredDataFunctions lists all data functions this task depends on. */
  requiredDataFunctions?: string[] | undefined;
}

/**
 * WorkflowEdge defines the relationship between two tasks in the workflow.
 * Edges are combined to form a workflow, or DAG.
 */
export interface WorkflowEdge {
  /** The name of the task going from */
  fromTaskName?:
    | string
    | undefined;
  /** The hash of the AST segment of the task */
  fromTaskHash?:
    | string
    | undefined;
  /** The worker that implements the task */
  fromTaskWorker?:
    | string
    | undefined;
  /** The name of the task going from */
  toTaskName?:
    | string
    | undefined;
  /** The hash of the AST segment of the task */
  toTaskHash?:
    | string
    | undefined;
  /** The worker that implements the task */
  toTaskWorker?: string | undefined;
}

/** Workflow defines a registered workflow, its task graph, and runtime settings. */
export interface Workflow {
  /** WorkflowName is the globally unique string identifier for this workflow. */
  workflowName?:
    | string
    | undefined;
  /** Description is a human-readable explanation of the workflow's purpose. */
  description?:
    | string
    | undefined;
  /**
   * WorkflowHash is a hash of the workflow structure, factoring in tasks,
   * dependencies, and execution models.
   */
  workflowHash?:
    | string
    | undefined;
  /** Edges defines the dependency relationships between tasks in this workflow. */
  edges?:
    | WorkflowEdge[]
    | undefined;
  /** ExecutionSettings defines concurrency limits and task prioritisation. */
  executionSettings?:
    | WorkflowExecutionSettings
    | undefined;
  /**
   * InputModel is a marshalled JSON schema describing the
   * parameters that must be provided at workflow trigger time.
   */
  inputModel?:
    | string
    | undefined;
  /**
   * HaltOnFailure instructs the orchestrator to stop parallel task execution
   * if any task encounters a failure.
   */
  haltOnFailure?: boolean | undefined;
}

/**
 * ============================================================
 * RegisterWorkerSnapshot RPC
 * ============================================================
 */
export interface RegisterWorkerRequest {
  /** Name is the globally unique name of the worker. */
  name?:
    | string
    | undefined;
  /** GitCommitHash is the current git commit */
  gitCommitHash?:
    | string
    | undefined;
  /** Datafunctions is an array of data functions */
  dataFunctions?:
    | DataFunction[]
    | undefined;
  /** Tasks is an array of tasks */
  tasks?:
    | Task[]
    | undefined;
  /** Workflows is an array of workflows */
  workflows?:
    | Workflow[]
    | undefined;
  /** An MD5 hash of the tasks and data functions */
  taskDfMd5Hash?: string | undefined;
}

/** RegisterWorkerResponse is the response message for the Registercodebase RPC. */
export interface RegisterWorkerResponse {
  /** Status indicates whether registration succeeded or failed. */
  status?:
    | RegistrationStatus
    | undefined;
  /** Message provides detail on why registration failed, if applicable. */
  message?: string | undefined;
}

/**
 * ============================================================
 * RegisterServingStatus RPC
 * ============================================================
 * RegisterServingStatus message
 */
export interface RegisterServingRequest {
  /** Name of the worker */
  name?:
    | string
    | undefined;
  /** Connection URL from the perspective of the core orchestrator */
  connectionUrl?:
    | string
    | undefined;
  /** Explicit flag stating whether the worker is serving */
  isServing?: boolean | undefined;
}

/** RegisterServingResponse message */
export interface RegisterServingResponse {
  /** Status indicates whether registration succeeded or failed. */
  status?:
    | RegistrationStatus
    | undefined;
  /** Message provides detail on why registration failed, if applicable. */
  message?: string | undefined;
}

/** TriggerWorkflowRequest is the request message for the TriggerWorkflow RPC. */
export interface TriggerWorkflowRequest {
  /** WorkflowName identifies the target workflow to execute. */
  workflowName?:
    | string
    | undefined;
  /** LogicalDate is the target execution timestamp. If unset, execution is immediate. */
  logicalDate?:
    | Date
    | undefined;
  /**
   * ExecutionParameters is a marshalled JSON struct of runtime parameters,
   * conforming to the workflow's ExecutionParametersModel schema.
   */
  executionParameters?:
    | string
    | undefined;
  /** TriggerSource identifies the origin of this trigger event. */
  triggerSource?: TriggerSource | undefined;
}

/** TriggerWorkflowResponse is the response message for the TriggerWorkflow RPC. */
export interface TriggerWorkflowResponse {
  /**
   * WorkflowRunID is the unique runtime execution tracker generated by the
   * orchestrator for this run.
   */
  workflowRunId?:
    | string
    | undefined;
  /** Status indicates whether the trigger was accepted or rejected. */
  status?:
    | TriggerStatus
    | undefined;
  /** Message provides detail on parameter or trigger validation failures. */
  message?: string | undefined;
}

/**
 * RegisterDataFunctionCompletionRequest is the request message for the
 * RegisterDataFunctionCompletion RPC.
 */
export interface RegisterDataFunctionCompletionRequest {
  /** WorkflowRunID is the ID of the workflow run that invoked the data function. */
  workflowRunId?:
    | string
    | undefined;
  /** Status indicates whether the data function completed successfully or failed. */
  status?:
    | ExecutionStatus
    | undefined;
  /**
   * Message is a client-provided description of the failure, ideally including
   * a full stack trace.
   */
  message?: string | undefined;
}

/**
 * RegisterDataFunctionCompletionResponse is the response message for the
 * RegisterDataFunctionCompletion RPC. Currently carries no fields.
 */
export interface RegisterDataFunctionCompletionResponse {
}

/** RegisterTaskResultRequest is the request message for the RegisterTaskResult RPC. */
export interface RegisterTaskResultRequest {
  /** WorkflowRunID is the ID of the workflow run this result belongs to. */
  workflowRunId?:
    | string
    | undefined;
  /** TaskName is the name of the task within the workflow. */
  taskName?:
    | string
    | undefined;
  /** Result is the task's output as a marshalled JSON object. Validated on retrieval. */
  result?:
    | string
    | undefined;
  /** Status indicates whether the task completed successfully or failed. */
  status?:
    | ExecutionStatus
    | undefined;
  /**
   * Message is the failure description produced by the worker, ideally a full
   * stack trace.
   */
  message?:
    | string
    | undefined;
  /** ComputeMetrics captures the CPU and memory resources consumed by this task. */
  computeMetrics?: ComputeMetrics | undefined;
}

/**
 * RegisterTaskResultResponse is the response message for the RegisterTaskResult RPC.
 * Currently carries no fields.
 */
export interface RegisterTaskResultResponse {
}

/** ExposeStateRequest is the request message for the ExposeState RPC. */
export interface ExposeStateRequest {
  /**
   * GitCommitHash, if provided, returns state as of that specific commit.
   * Takes priority over Timestamp when both are set.
   */
  gitCommitHash?:
    | string
    | undefined;
  /**
   * Timestamp, if provided, returns state as of the closest commit to that time.
   * Ignored if GitCommitHash is set.
   */
  timestamp?:
    | Date
    | undefined;
  /**
   * Codebase, if provided, filters out all assets (Tasks, Workflows,
   * DataFunctions, etc.) registered under this codebase from the response.
   */
  codebase?: string | undefined;
}

/** ExposeStateResponse is the response message for the ExposeState RPC. */
export interface ExposeStateResponse {
  /** Tasks is the array of registered tasks matching the requested state. */
  tasks?:
    | Task[]
    | undefined;
  /** Workflows is the array of registered workflows and their task references. */
  workflows?:
    | Workflow[]
    | undefined;
  /** DataFunctions is the array of registered data functions. */
  dataFunctions?: DataFunction[] | undefined;
}

/**
 * ============================================================
 * QueryTaskResult RPC
 * ============================================================
 *
 * The top-level request message for QueryTaskResult.
 */
export interface QueryParams {
  /** The name of the task to return results for. */
  name?:
    | string
    | undefined;
  /** Filters applied to execution parameters. Groups are AND-ed together. */
  executionParameterFilters?:
    | FilterGroup[]
    | undefined;
  /** Filters applied to task results. Groups are AND-ed together. */
  resultFilters?:
    | FilterGroup[]
    | undefined;
  /** Defines how results should be sorted. Evaluated in order. */
  orderBy?:
    | OrderByStatement[]
    | undefined;
  /** If set, only these keys will be included in each result's payload. */
  resultFields?:
    | string[]
    | undefined;
  /** The maximum number of results to return. Required. */
  pageSize?:
    | number
    | undefined;
  /** Opaque token from a previous response. Omit on the first request. */
  pageToken?: string | undefined;
}

/** The top-level response message for QueryTaskResult. */
export interface PastResults {
  /** The matched task results. */
  results?:
    | TaskResult[]
    | undefined;
  /**
   * Opaque token to pass as page_token in the next request.
   * Absent if there are no further results.
   */
  nextPageToken?: string | undefined;
}

/** A single task result entry. */
export interface TaskResult {
  /** The name of the task. */
  name?:
    | string
    | undefined;
  /** The execution parameters that triggered this task, as a marshalled JSON string. */
  executionParameters?:
    | string
    | undefined;
  /**
   * The task result, as a marshalled JSON string.
   * If result_fields was set on the request, only those fields will be present.
   */
  result?: string | undefined;
}

/** A set of LeafFilters joined by OR. All FilterGroups in a list are AND-ed together. */
export interface FilterGroup {
  /** At least one filter must match for this group to be satisfied. */
  filters?: LeafFilter[] | undefined;
}

/** A single condition evaluated against one KV pair. */
export interface LeafFilter {
  /** The key to filter on. */
  key?:
    | string
    | undefined;
  /** The comparison operation to apply. */
  comparator?:
    | Comparator
    | undefined;
  /**
   * The value(s) to compare against.
   * Omit for EXISTS and NOT_EXISTS.
   * Populate values (not value) for IN and NOT_IN.
   */
  valueOneof?:
    | //
    /** For EQ, NEQ, GT, GTE, LT, LTE, CONTAINS, PREFIX */
    { $case: "value"; value: string }
    | //
    /** For IN, NOT_IN */
    { $case: "values"; value: StringList }
    | undefined;
}

/** A wrapper for a list of strings, used for IN / NOT_IN comparators. */
export interface StringList {
  values?: string[] | undefined;
}

/** Defines a single sort statement. */
export interface OrderByStatement {
  /** The key to sort by. */
  key?:
    | string
    | undefined;
  /** Which table the key belongs to. */
  source?:
    | DataSource
    | undefined;
  /** The sort direction. */
  direction?: SortDirection | undefined;
}

function createBaseDataFunction(): DataFunction {
  return { name: "", hash: "", inputModel: "", outputModel: "", settings: undefined };
}

export const DataFunction: MessageFns<DataFunction> = {
  encode(message: DataFunction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.hash !== undefined && message.hash !== "") {
      writer.uint32(18).string(message.hash);
    }
    if (message.inputModel !== undefined && message.inputModel !== "") {
      writer.uint32(26).string(message.inputModel);
    }
    if (message.outputModel !== undefined && message.outputModel !== "") {
      writer.uint32(34).string(message.outputModel);
    }
    if (message.settings !== undefined) {
      DataFunctionSettings.encode(message.settings, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataFunction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataFunction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputModel = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.outputModel = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.settings = DataFunctionSettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataFunction {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      inputModel: isSet(object.inputModel) ? globalThis.String(object.inputModel) : "",
      outputModel: isSet(object.outputModel) ? globalThis.String(object.outputModel) : "",
      settings: isSet(object.settings) ? DataFunctionSettings.fromJSON(object.settings) : undefined,
    };
  },

  toJSON(message: DataFunction): unknown {
    const obj: any = {};
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.hash !== undefined && message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.inputModel !== undefined && message.inputModel !== "") {
      obj.inputModel = message.inputModel;
    }
    if (message.outputModel !== undefined && message.outputModel !== "") {
      obj.outputModel = message.outputModel;
    }
    if (message.settings !== undefined) {
      obj.settings = DataFunctionSettings.toJSON(message.settings);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataFunction>, I>>(base?: I): DataFunction {
    return DataFunction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataFunction>, I>>(object: I): DataFunction {
    const message = createBaseDataFunction();
    message.name = object.name ?? "";
    message.hash = object.hash ?? "";
    message.inputModel = object.inputModel ?? "";
    message.outputModel = object.outputModel ?? "";
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? DataFunctionSettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseTask(): Task {
  return {
    taskHash: "",
    name: "",
    description: "",
    executionSettings: undefined,
    inputModel: "",
    outputModel: "",
    requiredDataFunctions: [],
  };
}

export const Task: MessageFns<Task> = {
  encode(message: Task, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskHash !== undefined && message.taskHash !== "") {
      writer.uint32(10).string(message.taskHash);
    }
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined && message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.executionSettings !== undefined) {
      TaskExecutionSettings.encode(message.executionSettings, writer.uint32(34).fork()).join();
    }
    if (message.inputModel !== undefined && message.inputModel !== "") {
      writer.uint32(42).string(message.inputModel);
    }
    if (message.outputModel !== undefined && message.outputModel !== "") {
      writer.uint32(50).string(message.outputModel);
    }
    if (message.requiredDataFunctions !== undefined && message.requiredDataFunctions.length !== 0) {
      for (const v of message.requiredDataFunctions) {
        writer.uint32(58).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Task {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTask();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskHash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.executionSettings = TaskExecutionSettings.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.inputModel = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.outputModel = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.requiredDataFunctions!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Task {
    return {
      taskHash: isSet(object.taskHash) ? globalThis.String(object.taskHash) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      executionSettings: isSet(object.executionSettings)
        ? TaskExecutionSettings.fromJSON(object.executionSettings)
        : undefined,
      inputModel: isSet(object.inputModel) ? globalThis.String(object.inputModel) : "",
      outputModel: isSet(object.outputModel) ? globalThis.String(object.outputModel) : "",
      requiredDataFunctions: globalThis.Array.isArray(object?.requiredDataFunctions)
        ? object.requiredDataFunctions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: Task): unknown {
    const obj: any = {};
    if (message.taskHash !== undefined && message.taskHash !== "") {
      obj.taskHash = message.taskHash;
    }
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== undefined && message.description !== "") {
      obj.description = message.description;
    }
    if (message.executionSettings !== undefined) {
      obj.executionSettings = TaskExecutionSettings.toJSON(message.executionSettings);
    }
    if (message.inputModel !== undefined && message.inputModel !== "") {
      obj.inputModel = message.inputModel;
    }
    if (message.outputModel !== undefined && message.outputModel !== "") {
      obj.outputModel = message.outputModel;
    }
    if (message.requiredDataFunctions?.length) {
      obj.requiredDataFunctions = message.requiredDataFunctions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Task>, I>>(base?: I): Task {
    return Task.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Task>, I>>(object: I): Task {
    const message = createBaseTask();
    message.taskHash = object.taskHash ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.executionSettings = (object.executionSettings !== undefined && object.executionSettings !== null)
      ? TaskExecutionSettings.fromPartial(object.executionSettings)
      : undefined;
    message.inputModel = object.inputModel ?? "";
    message.outputModel = object.outputModel ?? "";
    message.requiredDataFunctions = object.requiredDataFunctions?.map((e) => e) || [];
    return message;
  },
};

function createBaseWorkflowEdge(): WorkflowEdge {
  return { fromTaskName: "", fromTaskHash: "", fromTaskWorker: "", toTaskName: "", toTaskHash: "", toTaskWorker: "" };
}

export const WorkflowEdge: MessageFns<WorkflowEdge> = {
  encode(message: WorkflowEdge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fromTaskName !== undefined && message.fromTaskName !== "") {
      writer.uint32(10).string(message.fromTaskName);
    }
    if (message.fromTaskHash !== undefined && message.fromTaskHash !== "") {
      writer.uint32(18).string(message.fromTaskHash);
    }
    if (message.fromTaskWorker !== undefined && message.fromTaskWorker !== "") {
      writer.uint32(26).string(message.fromTaskWorker);
    }
    if (message.toTaskName !== undefined && message.toTaskName !== "") {
      writer.uint32(34).string(message.toTaskName);
    }
    if (message.toTaskHash !== undefined && message.toTaskHash !== "") {
      writer.uint32(42).string(message.toTaskHash);
    }
    if (message.toTaskWorker !== undefined && message.toTaskWorker !== "") {
      writer.uint32(50).string(message.toTaskWorker);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowEdge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowEdge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fromTaskName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fromTaskHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fromTaskWorker = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toTaskName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.toTaskHash = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.toTaskWorker = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowEdge {
    return {
      fromTaskName: isSet(object.fromTaskName) ? globalThis.String(object.fromTaskName) : "",
      fromTaskHash: isSet(object.fromTaskHash) ? globalThis.String(object.fromTaskHash) : "",
      fromTaskWorker: isSet(object.fromTaskWorker) ? globalThis.String(object.fromTaskWorker) : "",
      toTaskName: isSet(object.toTaskName) ? globalThis.String(object.toTaskName) : "",
      toTaskHash: isSet(object.toTaskHash) ? globalThis.String(object.toTaskHash) : "",
      toTaskWorker: isSet(object.toTaskWorker) ? globalThis.String(object.toTaskWorker) : "",
    };
  },

  toJSON(message: WorkflowEdge): unknown {
    const obj: any = {};
    if (message.fromTaskName !== undefined && message.fromTaskName !== "") {
      obj.fromTaskName = message.fromTaskName;
    }
    if (message.fromTaskHash !== undefined && message.fromTaskHash !== "") {
      obj.fromTaskHash = message.fromTaskHash;
    }
    if (message.fromTaskWorker !== undefined && message.fromTaskWorker !== "") {
      obj.fromTaskWorker = message.fromTaskWorker;
    }
    if (message.toTaskName !== undefined && message.toTaskName !== "") {
      obj.toTaskName = message.toTaskName;
    }
    if (message.toTaskHash !== undefined && message.toTaskHash !== "") {
      obj.toTaskHash = message.toTaskHash;
    }
    if (message.toTaskWorker !== undefined && message.toTaskWorker !== "") {
      obj.toTaskWorker = message.toTaskWorker;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowEdge>, I>>(base?: I): WorkflowEdge {
    return WorkflowEdge.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowEdge>, I>>(object: I): WorkflowEdge {
    const message = createBaseWorkflowEdge();
    message.fromTaskName = object.fromTaskName ?? "";
    message.fromTaskHash = object.fromTaskHash ?? "";
    message.fromTaskWorker = object.fromTaskWorker ?? "";
    message.toTaskName = object.toTaskName ?? "";
    message.toTaskHash = object.toTaskHash ?? "";
    message.toTaskWorker = object.toTaskWorker ?? "";
    return message;
  },
};

function createBaseWorkflow(): Workflow {
  return {
    workflowName: "",
    description: "",
    workflowHash: "",
    edges: [],
    executionSettings: undefined,
    inputModel: "",
    haltOnFailure: false,
  };
}

export const Workflow: MessageFns<Workflow> = {
  encode(message: Workflow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowName !== undefined && message.workflowName !== "") {
      writer.uint32(10).string(message.workflowName);
    }
    if (message.description !== undefined && message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.workflowHash !== undefined && message.workflowHash !== "") {
      writer.uint32(26).string(message.workflowHash);
    }
    if (message.edges !== undefined && message.edges.length !== 0) {
      for (const v of message.edges) {
        WorkflowEdge.encode(v!, writer.uint32(34).fork()).join();
      }
    }
    if (message.executionSettings !== undefined) {
      WorkflowExecutionSettings.encode(message.executionSettings, writer.uint32(42).fork()).join();
    }
    if (message.inputModel !== undefined && message.inputModel !== "") {
      writer.uint32(50).string(message.inputModel);
    }
    if (message.haltOnFailure !== undefined && message.haltOnFailure !== false) {
      writer.uint32(56).bool(message.haltOnFailure);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Workflow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workflowHash = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const el = WorkflowEdge.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.edges!.push(el);
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.executionSettings = WorkflowExecutionSettings.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.inputModel = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.haltOnFailure = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Workflow {
    return {
      workflowName: isSet(object.workflowName) ? globalThis.String(object.workflowName) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      workflowHash: isSet(object.workflowHash) ? globalThis.String(object.workflowHash) : "",
      edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => WorkflowEdge.fromJSON(e)) : [],
      executionSettings: isSet(object.executionSettings)
        ? WorkflowExecutionSettings.fromJSON(object.executionSettings)
        : undefined,
      inputModel: isSet(object.inputModel) ? globalThis.String(object.inputModel) : "",
      haltOnFailure: isSet(object.haltOnFailure) ? globalThis.Boolean(object.haltOnFailure) : false,
    };
  },

  toJSON(message: Workflow): unknown {
    const obj: any = {};
    if (message.workflowName !== undefined && message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.description !== undefined && message.description !== "") {
      obj.description = message.description;
    }
    if (message.workflowHash !== undefined && message.workflowHash !== "") {
      obj.workflowHash = message.workflowHash;
    }
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => WorkflowEdge.toJSON(e));
    }
    if (message.executionSettings !== undefined) {
      obj.executionSettings = WorkflowExecutionSettings.toJSON(message.executionSettings);
    }
    if (message.inputModel !== undefined && message.inputModel !== "") {
      obj.inputModel = message.inputModel;
    }
    if (message.haltOnFailure !== undefined && message.haltOnFailure !== false) {
      obj.haltOnFailure = message.haltOnFailure;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Workflow>, I>>(base?: I): Workflow {
    return Workflow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Workflow>, I>>(object: I): Workflow {
    const message = createBaseWorkflow();
    message.workflowName = object.workflowName ?? "";
    message.description = object.description ?? "";
    message.workflowHash = object.workflowHash ?? "";
    message.edges = object.edges?.map((e) => WorkflowEdge.fromPartial(e)) || [];
    message.executionSettings = (object.executionSettings !== undefined && object.executionSettings !== null)
      ? WorkflowExecutionSettings.fromPartial(object.executionSettings)
      : undefined;
    message.inputModel = object.inputModel ?? "";
    message.haltOnFailure = object.haltOnFailure ?? false;
    return message;
  },
};

function createBaseRegisterWorkerRequest(): RegisterWorkerRequest {
  return { name: "", gitCommitHash: "", dataFunctions: [], tasks: [], workflows: [], taskDfMd5Hash: "" };
}

export const RegisterWorkerRequest: MessageFns<RegisterWorkerRequest> = {
  encode(message: RegisterWorkerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.gitCommitHash !== undefined && message.gitCommitHash !== "") {
      writer.uint32(18).string(message.gitCommitHash);
    }
    if (message.dataFunctions !== undefined && message.dataFunctions.length !== 0) {
      for (const v of message.dataFunctions) {
        DataFunction.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.tasks !== undefined && message.tasks.length !== 0) {
      for (const v of message.tasks) {
        Task.encode(v!, writer.uint32(34).fork()).join();
      }
    }
    if (message.workflows !== undefined && message.workflows.length !== 0) {
      for (const v of message.workflows) {
        Workflow.encode(v!, writer.uint32(42).fork()).join();
      }
    }
    if (message.taskDfMd5Hash !== undefined && message.taskDfMd5Hash !== "") {
      writer.uint32(50).string(message.taskDfMd5Hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gitCommitHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = DataFunction.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.dataFunctions!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const el = Task.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.tasks!.push(el);
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const el = Workflow.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.workflows!.push(el);
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.taskDfMd5Hash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkerRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      gitCommitHash: isSet(object.gitCommitHash) ? globalThis.String(object.gitCommitHash) : "",
      dataFunctions: globalThis.Array.isArray(object?.dataFunctions)
        ? object.dataFunctions.map((e: any) => DataFunction.fromJSON(e))
        : [],
      tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => Task.fromJSON(e)) : [],
      workflows: globalThis.Array.isArray(object?.workflows)
        ? object.workflows.map((e: any) => Workflow.fromJSON(e))
        : [],
      taskDfMd5Hash: isSet(object.taskDfMd5Hash) ? globalThis.String(object.taskDfMd5Hash) : "",
    };
  },

  toJSON(message: RegisterWorkerRequest): unknown {
    const obj: any = {};
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.gitCommitHash !== undefined && message.gitCommitHash !== "") {
      obj.gitCommitHash = message.gitCommitHash;
    }
    if (message.dataFunctions?.length) {
      obj.dataFunctions = message.dataFunctions.map((e) => DataFunction.toJSON(e));
    }
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => Task.toJSON(e));
    }
    if (message.workflows?.length) {
      obj.workflows = message.workflows.map((e) => Workflow.toJSON(e));
    }
    if (message.taskDfMd5Hash !== undefined && message.taskDfMd5Hash !== "") {
      obj.taskDfMd5Hash = message.taskDfMd5Hash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkerRequest>, I>>(base?: I): RegisterWorkerRequest {
    return RegisterWorkerRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkerRequest>, I>>(object: I): RegisterWorkerRequest {
    const message = createBaseRegisterWorkerRequest();
    message.name = object.name ?? "";
    message.gitCommitHash = object.gitCommitHash ?? "";
    message.dataFunctions = object.dataFunctions?.map((e) => DataFunction.fromPartial(e)) || [];
    message.tasks = object.tasks?.map((e) => Task.fromPartial(e)) || [];
    message.workflows = object.workflows?.map((e) => Workflow.fromPartial(e)) || [];
    message.taskDfMd5Hash = object.taskDfMd5Hash ?? "";
    return message;
  },
};

function createBaseRegisterWorkerResponse(): RegisterWorkerResponse {
  return { status: 0, message: "" };
}

export const RegisterWorkerResponse: MessageFns<RegisterWorkerResponse> = {
  encode(message: RegisterWorkerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined && message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkerResponse {
    return {
      status: isSet(object.status) ? registrationStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RegisterWorkerResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined && message.status !== 0) {
      obj.status = registrationStatusToJSON(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkerResponse>, I>>(base?: I): RegisterWorkerResponse {
    return RegisterWorkerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkerResponse>, I>>(object: I): RegisterWorkerResponse {
    const message = createBaseRegisterWorkerResponse();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRegisterServingRequest(): RegisterServingRequest {
  return { name: "", connectionUrl: "", isServing: false };
}

export const RegisterServingRequest: MessageFns<RegisterServingRequest> = {
  encode(message: RegisterServingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.connectionUrl !== undefined && message.connectionUrl !== "") {
      writer.uint32(18).string(message.connectionUrl);
    }
    if (message.isServing !== undefined && message.isServing !== false) {
      writer.uint32(24).bool(message.isServing);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterServingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterServingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.connectionUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isServing = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterServingRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      connectionUrl: isSet(object.connectionUrl) ? globalThis.String(object.connectionUrl) : "",
      isServing: isSet(object.isServing) ? globalThis.Boolean(object.isServing) : false,
    };
  },

  toJSON(message: RegisterServingRequest): unknown {
    const obj: any = {};
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.connectionUrl !== undefined && message.connectionUrl !== "") {
      obj.connectionUrl = message.connectionUrl;
    }
    if (message.isServing !== undefined && message.isServing !== false) {
      obj.isServing = message.isServing;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterServingRequest>, I>>(base?: I): RegisterServingRequest {
    return RegisterServingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterServingRequest>, I>>(object: I): RegisterServingRequest {
    const message = createBaseRegisterServingRequest();
    message.name = object.name ?? "";
    message.connectionUrl = object.connectionUrl ?? "";
    message.isServing = object.isServing ?? false;
    return message;
  },
};

function createBaseRegisterServingResponse(): RegisterServingResponse {
  return { status: 0, message: "" };
}

export const RegisterServingResponse: MessageFns<RegisterServingResponse> = {
  encode(message: RegisterServingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined && message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterServingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterServingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterServingResponse {
    return {
      status: isSet(object.status) ? registrationStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RegisterServingResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined && message.status !== 0) {
      obj.status = registrationStatusToJSON(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterServingResponse>, I>>(base?: I): RegisterServingResponse {
    return RegisterServingResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterServingResponse>, I>>(object: I): RegisterServingResponse {
    const message = createBaseRegisterServingResponse();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseTriggerWorkflowRequest(): TriggerWorkflowRequest {
  return { workflowName: "", logicalDate: undefined, executionParameters: "", triggerSource: 0 };
}

export const TriggerWorkflowRequest: MessageFns<TriggerWorkflowRequest> = {
  encode(message: TriggerWorkflowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowName !== undefined && message.workflowName !== "") {
      writer.uint32(10).string(message.workflowName);
    }
    if (message.logicalDate !== undefined) {
      Timestamp.encode(toTimestamp(message.logicalDate), writer.uint32(18).fork()).join();
    }
    if (message.executionParameters !== undefined && message.executionParameters !== "") {
      writer.uint32(26).string(message.executionParameters);
    }
    if (message.triggerSource !== undefined && message.triggerSource !== 0) {
      writer.uint32(32).int32(message.triggerSource);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TriggerWorkflowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerWorkflowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logicalDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.executionParameters = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.triggerSource = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerWorkflowRequest {
    return {
      workflowName: isSet(object.workflowName) ? globalThis.String(object.workflowName) : "",
      logicalDate: isSet(object.logicalDate) ? fromJsonTimestamp(object.logicalDate) : undefined,
      executionParameters: isSet(object.executionParameters) ? globalThis.String(object.executionParameters) : "",
      triggerSource: isSet(object.triggerSource) ? triggerSourceFromJSON(object.triggerSource) : 0,
    };
  },

  toJSON(message: TriggerWorkflowRequest): unknown {
    const obj: any = {};
    if (message.workflowName !== undefined && message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.logicalDate !== undefined) {
      obj.logicalDate = message.logicalDate.toISOString();
    }
    if (message.executionParameters !== undefined && message.executionParameters !== "") {
      obj.executionParameters = message.executionParameters;
    }
    if (message.triggerSource !== undefined && message.triggerSource !== 0) {
      obj.triggerSource = triggerSourceToJSON(message.triggerSource);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TriggerWorkflowRequest>, I>>(base?: I): TriggerWorkflowRequest {
    return TriggerWorkflowRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TriggerWorkflowRequest>, I>>(object: I): TriggerWorkflowRequest {
    const message = createBaseTriggerWorkflowRequest();
    message.workflowName = object.workflowName ?? "";
    message.logicalDate = object.logicalDate ?? undefined;
    message.executionParameters = object.executionParameters ?? "";
    message.triggerSource = object.triggerSource ?? 0;
    return message;
  },
};

function createBaseTriggerWorkflowResponse(): TriggerWorkflowResponse {
  return { workflowRunId: "", status: 0, message: "" };
}

export const TriggerWorkflowResponse: MessageFns<TriggerWorkflowResponse> = {
  encode(message: TriggerWorkflowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowRunId !== undefined && message.workflowRunId !== "") {
      writer.uint32(10).string(message.workflowRunId);
    }
    if (message.status !== undefined && message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TriggerWorkflowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerWorkflowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowRunId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerWorkflowResponse {
    return {
      workflowRunId: isSet(object.workflowRunId) ? globalThis.String(object.workflowRunId) : "",
      status: isSet(object.status) ? triggerStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: TriggerWorkflowResponse): unknown {
    const obj: any = {};
    if (message.workflowRunId !== undefined && message.workflowRunId !== "") {
      obj.workflowRunId = message.workflowRunId;
    }
    if (message.status !== undefined && message.status !== 0) {
      obj.status = triggerStatusToJSON(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TriggerWorkflowResponse>, I>>(base?: I): TriggerWorkflowResponse {
    return TriggerWorkflowResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TriggerWorkflowResponse>, I>>(object: I): TriggerWorkflowResponse {
    const message = createBaseTriggerWorkflowResponse();
    message.workflowRunId = object.workflowRunId ?? "";
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRegisterDataFunctionCompletionRequest(): RegisterDataFunctionCompletionRequest {
  return { workflowRunId: "", status: 0, message: "" };
}

export const RegisterDataFunctionCompletionRequest: MessageFns<RegisterDataFunctionCompletionRequest> = {
  encode(message: RegisterDataFunctionCompletionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowRunId !== undefined && message.workflowRunId !== "") {
      writer.uint32(10).string(message.workflowRunId);
    }
    if (message.status !== undefined && message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterDataFunctionCompletionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDataFunctionCompletionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowRunId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterDataFunctionCompletionRequest {
    return {
      workflowRunId: isSet(object.workflowRunId) ? globalThis.String(object.workflowRunId) : "",
      status: isSet(object.status) ? executionStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RegisterDataFunctionCompletionRequest): unknown {
    const obj: any = {};
    if (message.workflowRunId !== undefined && message.workflowRunId !== "") {
      obj.workflowRunId = message.workflowRunId;
    }
    if (message.status !== undefined && message.status !== 0) {
      obj.status = executionStatusToJSON(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDataFunctionCompletionRequest>, I>>(
    base?: I,
  ): RegisterDataFunctionCompletionRequest {
    return RegisterDataFunctionCompletionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDataFunctionCompletionRequest>, I>>(
    object: I,
  ): RegisterDataFunctionCompletionRequest {
    const message = createBaseRegisterDataFunctionCompletionRequest();
    message.workflowRunId = object.workflowRunId ?? "";
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRegisterDataFunctionCompletionResponse(): RegisterDataFunctionCompletionResponse {
  return {};
}

export const RegisterDataFunctionCompletionResponse: MessageFns<RegisterDataFunctionCompletionResponse> = {
  encode(_: RegisterDataFunctionCompletionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterDataFunctionCompletionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDataFunctionCompletionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RegisterDataFunctionCompletionResponse {
    return {};
  },

  toJSON(_: RegisterDataFunctionCompletionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDataFunctionCompletionResponse>, I>>(
    base?: I,
  ): RegisterDataFunctionCompletionResponse {
    return RegisterDataFunctionCompletionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDataFunctionCompletionResponse>, I>>(
    _: I,
  ): RegisterDataFunctionCompletionResponse {
    const message = createBaseRegisterDataFunctionCompletionResponse();
    return message;
  },
};

function createBaseRegisterTaskResultRequest(): RegisterTaskResultRequest {
  return { workflowRunId: "", taskName: "", result: "", status: 0, message: "", computeMetrics: undefined };
}

export const RegisterTaskResultRequest: MessageFns<RegisterTaskResultRequest> = {
  encode(message: RegisterTaskResultRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowRunId !== undefined && message.workflowRunId !== "") {
      writer.uint32(10).string(message.workflowRunId);
    }
    if (message.taskName !== undefined && message.taskName !== "") {
      writer.uint32(18).string(message.taskName);
    }
    if (message.result !== undefined && message.result !== "") {
      writer.uint32(26).string(message.result);
    }
    if (message.status !== undefined && message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      writer.uint32(42).string(message.message);
    }
    if (message.computeMetrics !== undefined) {
      ComputeMetrics.encode(message.computeMetrics, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterTaskResultRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterTaskResultRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowRunId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.taskName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.result = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.computeMetrics = ComputeMetrics.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterTaskResultRequest {
    return {
      workflowRunId: isSet(object.workflowRunId) ? globalThis.String(object.workflowRunId) : "",
      taskName: isSet(object.taskName) ? globalThis.String(object.taskName) : "",
      result: isSet(object.result) ? globalThis.String(object.result) : "",
      status: isSet(object.status) ? executionStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      computeMetrics: isSet(object.computeMetrics) ? ComputeMetrics.fromJSON(object.computeMetrics) : undefined,
    };
  },

  toJSON(message: RegisterTaskResultRequest): unknown {
    const obj: any = {};
    if (message.workflowRunId !== undefined && message.workflowRunId !== "") {
      obj.workflowRunId = message.workflowRunId;
    }
    if (message.taskName !== undefined && message.taskName !== "") {
      obj.taskName = message.taskName;
    }
    if (message.result !== undefined && message.result !== "") {
      obj.result = message.result;
    }
    if (message.status !== undefined && message.status !== 0) {
      obj.status = executionStatusToJSON(message.status);
    }
    if (message.message !== undefined && message.message !== "") {
      obj.message = message.message;
    }
    if (message.computeMetrics !== undefined) {
      obj.computeMetrics = ComputeMetrics.toJSON(message.computeMetrics);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterTaskResultRequest>, I>>(base?: I): RegisterTaskResultRequest {
    return RegisterTaskResultRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterTaskResultRequest>, I>>(object: I): RegisterTaskResultRequest {
    const message = createBaseRegisterTaskResultRequest();
    message.workflowRunId = object.workflowRunId ?? "";
    message.taskName = object.taskName ?? "";
    message.result = object.result ?? "";
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    message.computeMetrics = (object.computeMetrics !== undefined && object.computeMetrics !== null)
      ? ComputeMetrics.fromPartial(object.computeMetrics)
      : undefined;
    return message;
  },
};

function createBaseRegisterTaskResultResponse(): RegisterTaskResultResponse {
  return {};
}

export const RegisterTaskResultResponse: MessageFns<RegisterTaskResultResponse> = {
  encode(_: RegisterTaskResultResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterTaskResultResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterTaskResultResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RegisterTaskResultResponse {
    return {};
  },

  toJSON(_: RegisterTaskResultResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterTaskResultResponse>, I>>(base?: I): RegisterTaskResultResponse {
    return RegisterTaskResultResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterTaskResultResponse>, I>>(_: I): RegisterTaskResultResponse {
    const message = createBaseRegisterTaskResultResponse();
    return message;
  },
};

function createBaseExposeStateRequest(): ExposeStateRequest {
  return { gitCommitHash: undefined, timestamp: undefined, codebase: undefined };
}

export const ExposeStateRequest: MessageFns<ExposeStateRequest> = {
  encode(message: ExposeStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gitCommitHash !== undefined) {
      writer.uint32(10).string(message.gitCommitHash);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(18).fork()).join();
    }
    if (message.codebase !== undefined) {
      writer.uint32(26).string(message.codebase);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExposeStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExposeStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gitCommitHash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.codebase = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExposeStateRequest {
    return {
      gitCommitHash: isSet(object.gitCommitHash) ? globalThis.String(object.gitCommitHash) : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      codebase: isSet(object.codebase) ? globalThis.String(object.codebase) : undefined,
    };
  },

  toJSON(message: ExposeStateRequest): unknown {
    const obj: any = {};
    if (message.gitCommitHash !== undefined) {
      obj.gitCommitHash = message.gitCommitHash;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.codebase !== undefined) {
      obj.codebase = message.codebase;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExposeStateRequest>, I>>(base?: I): ExposeStateRequest {
    return ExposeStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExposeStateRequest>, I>>(object: I): ExposeStateRequest {
    const message = createBaseExposeStateRequest();
    message.gitCommitHash = object.gitCommitHash ?? undefined;
    message.timestamp = object.timestamp ?? undefined;
    message.codebase = object.codebase ?? undefined;
    return message;
  },
};

function createBaseExposeStateResponse(): ExposeStateResponse {
  return { tasks: [], workflows: [], dataFunctions: [] };
}

export const ExposeStateResponse: MessageFns<ExposeStateResponse> = {
  encode(message: ExposeStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tasks !== undefined && message.tasks.length !== 0) {
      for (const v of message.tasks) {
        Task.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.workflows !== undefined && message.workflows.length !== 0) {
      for (const v of message.workflows) {
        Workflow.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.dataFunctions !== undefined && message.dataFunctions.length !== 0) {
      for (const v of message.dataFunctions) {
        DataFunction.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExposeStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExposeStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = Task.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.tasks!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = Workflow.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.workflows!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = DataFunction.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.dataFunctions!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExposeStateResponse {
    return {
      tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => Task.fromJSON(e)) : [],
      workflows: globalThis.Array.isArray(object?.workflows)
        ? object.workflows.map((e: any) => Workflow.fromJSON(e))
        : [],
      dataFunctions: globalThis.Array.isArray(object?.dataFunctions)
        ? object.dataFunctions.map((e: any) => DataFunction.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ExposeStateResponse): unknown {
    const obj: any = {};
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => Task.toJSON(e));
    }
    if (message.workflows?.length) {
      obj.workflows = message.workflows.map((e) => Workflow.toJSON(e));
    }
    if (message.dataFunctions?.length) {
      obj.dataFunctions = message.dataFunctions.map((e) => DataFunction.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExposeStateResponse>, I>>(base?: I): ExposeStateResponse {
    return ExposeStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExposeStateResponse>, I>>(object: I): ExposeStateResponse {
    const message = createBaseExposeStateResponse();
    message.tasks = object.tasks?.map((e) => Task.fromPartial(e)) || [];
    message.workflows = object.workflows?.map((e) => Workflow.fromPartial(e)) || [];
    message.dataFunctions = object.dataFunctions?.map((e) => DataFunction.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryParams(): QueryParams {
  return {
    name: "",
    executionParameterFilters: [],
    resultFilters: [],
    orderBy: [],
    resultFields: [],
    pageSize: 0,
    pageToken: undefined,
  };
}

export const QueryParams: MessageFns<QueryParams> = {
  encode(message: QueryParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.executionParameterFilters !== undefined && message.executionParameterFilters.length !== 0) {
      for (const v of message.executionParameterFilters) {
        FilterGroup.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.resultFilters !== undefined && message.resultFilters.length !== 0) {
      for (const v of message.resultFilters) {
        FilterGroup.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.orderBy !== undefined && message.orderBy.length !== 0) {
      for (const v of message.orderBy) {
        OrderByStatement.encode(v!, writer.uint32(34).fork()).join();
      }
    }
    if (message.resultFields !== undefined && message.resultFields.length !== 0) {
      for (const v of message.resultFields) {
        writer.uint32(42).string(v!);
      }
    }
    if (message.pageSize !== undefined && message.pageSize !== 0) {
      writer.uint32(48).int32(message.pageSize);
    }
    if (message.pageToken !== undefined) {
      writer.uint32(58).string(message.pageToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = FilterGroup.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.executionParameterFilters!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = FilterGroup.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.resultFilters!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const el = OrderByStatement.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.orderBy!.push(el);
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.resultFields!.push(el);
          }
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.pageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParams {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      executionParameterFilters: globalThis.Array.isArray(object?.executionParameterFilters)
        ? object.executionParameterFilters.map((e: any) => FilterGroup.fromJSON(e))
        : globalThis.Array.isArray(object?.execution_parameter_filters)
        ? object.execution_parameter_filters.map((e: any) => FilterGroup.fromJSON(e))
        : [],
      resultFilters: globalThis.Array.isArray(object?.resultFilters)
        ? object.resultFilters.map((e: any) => FilterGroup.fromJSON(e))
        : globalThis.Array.isArray(object?.result_filters)
        ? object.result_filters.map((e: any) => FilterGroup.fromJSON(e))
        : [],
      orderBy: globalThis.Array.isArray(object?.orderBy)
        ? object.orderBy.map((e: any) => OrderByStatement.fromJSON(e))
        : globalThis.Array.isArray(object?.order_by)
        ? object.order_by.map((e: any) => OrderByStatement.fromJSON(e))
        : [],
      resultFields: globalThis.Array.isArray(object?.resultFields)
        ? object.resultFields.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.result_fields)
        ? object.result_fields.map((e: any) => globalThis.String(e))
        : [],
      pageSize: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : 0,
      pageToken: isSet(object.pageToken)
        ? globalThis.String(object.pageToken)
        : isSet(object.page_token)
        ? globalThis.String(object.page_token)
        : undefined,
    };
  },

  toJSON(message: QueryParams): unknown {
    const obj: any = {};
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.executionParameterFilters?.length) {
      obj.executionParameterFilters = message.executionParameterFilters.map((e) => FilterGroup.toJSON(e));
    }
    if (message.resultFilters?.length) {
      obj.resultFilters = message.resultFilters.map((e) => FilterGroup.toJSON(e));
    }
    if (message.orderBy?.length) {
      obj.orderBy = message.orderBy.map((e) => OrderByStatement.toJSON(e));
    }
    if (message.resultFields?.length) {
      obj.resultFields = message.resultFields;
    }
    if (message.pageSize !== undefined && message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== undefined) {
      obj.pageToken = message.pageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParams>, I>>(base?: I): QueryParams {
    return QueryParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParams>, I>>(object: I): QueryParams {
    const message = createBaseQueryParams();
    message.name = object.name ?? "";
    message.executionParameterFilters = object.executionParameterFilters?.map((e) => FilterGroup.fromPartial(e)) || [];
    message.resultFilters = object.resultFilters?.map((e) => FilterGroup.fromPartial(e)) || [];
    message.orderBy = object.orderBy?.map((e) => OrderByStatement.fromPartial(e)) || [];
    message.resultFields = object.resultFields?.map((e) => e) || [];
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? undefined;
    return message;
  },
};

function createBasePastResults(): PastResults {
  return { results: [], nextPageToken: undefined };
}

export const PastResults: MessageFns<PastResults> = {
  encode(message: PastResults, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.results !== undefined && message.results.length !== 0) {
      for (const v of message.results) {
        TaskResult.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.nextPageToken !== undefined) {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PastResults {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePastResults();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = TaskResult.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.results!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PastResults {
    return {
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => TaskResult.fromJSON(e)) : [],
      nextPageToken: isSet(object.nextPageToken)
        ? globalThis.String(object.nextPageToken)
        : isSet(object.next_page_token)
        ? globalThis.String(object.next_page_token)
        : undefined,
    };
  },

  toJSON(message: PastResults): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => TaskResult.toJSON(e));
    }
    if (message.nextPageToken !== undefined) {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PastResults>, I>>(base?: I): PastResults {
    return PastResults.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PastResults>, I>>(object: I): PastResults {
    const message = createBasePastResults();
    message.results = object.results?.map((e) => TaskResult.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? undefined;
    return message;
  },
};

function createBaseTaskResult(): TaskResult {
  return { name: "", executionParameters: "", result: "" };
}

export const TaskResult: MessageFns<TaskResult> = {
  encode(message: TaskResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.executionParameters !== undefined && message.executionParameters !== "") {
      writer.uint32(18).string(message.executionParameters);
    }
    if (message.result !== undefined && message.result !== "") {
      writer.uint32(26).string(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.executionParameters = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.result = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskResult {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      executionParameters: isSet(object.executionParameters)
        ? globalThis.String(object.executionParameters)
        : isSet(object.execution_parameters)
        ? globalThis.String(object.execution_parameters)
        : "",
      result: isSet(object.result) ? globalThis.String(object.result) : "",
    };
  },

  toJSON(message: TaskResult): unknown {
    const obj: any = {};
    if (message.name !== undefined && message.name !== "") {
      obj.name = message.name;
    }
    if (message.executionParameters !== undefined && message.executionParameters !== "") {
      obj.executionParameters = message.executionParameters;
    }
    if (message.result !== undefined && message.result !== "") {
      obj.result = message.result;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaskResult>, I>>(base?: I): TaskResult {
    return TaskResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaskResult>, I>>(object: I): TaskResult {
    const message = createBaseTaskResult();
    message.name = object.name ?? "";
    message.executionParameters = object.executionParameters ?? "";
    message.result = object.result ?? "";
    return message;
  },
};

function createBaseFilterGroup(): FilterGroup {
  return { filters: [] };
}

export const FilterGroup: MessageFns<FilterGroup> = {
  encode(message: FilterGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filters !== undefined && message.filters.length !== 0) {
      for (const v of message.filters) {
        LeafFilter.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilterGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilterGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = LeafFilter.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.filters!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilterGroup {
    return {
      filters: globalThis.Array.isArray(object?.filters) ? object.filters.map((e: any) => LeafFilter.fromJSON(e)) : [],
    };
  },

  toJSON(message: FilterGroup): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => LeafFilter.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilterGroup>, I>>(base?: I): FilterGroup {
    return FilterGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilterGroup>, I>>(object: I): FilterGroup {
    const message = createBaseFilterGroup();
    message.filters = object.filters?.map((e) => LeafFilter.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLeafFilter(): LeafFilter {
  return { key: "", comparator: 0, valueOneof: undefined };
}

export const LeafFilter: MessageFns<LeafFilter> = {
  encode(message: LeafFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined && message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.comparator !== undefined && message.comparator !== 0) {
      writer.uint32(16).int32(message.comparator);
    }
    switch (message.valueOneof?.$case) {
      case "value":
        writer.uint32(26).string(message.valueOneof.value);
        break;
      case "values":
        StringList.encode(message.valueOneof.value, writer.uint32(34).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LeafFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeafFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.comparator = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.valueOneof = { $case: "value", value: reader.string() };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.valueOneof = { $case: "values", value: StringList.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeafFilter {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      comparator: isSet(object.comparator) ? comparatorFromJSON(object.comparator) : 0,
      valueOneof: isSet(object.value)
        ? { $case: "value", value: globalThis.String(object.value) }
        : isSet(object.values)
        ? { $case: "values", value: StringList.fromJSON(object.values) }
        : undefined,
    };
  },

  toJSON(message: LeafFilter): unknown {
    const obj: any = {};
    if (message.key !== undefined && message.key !== "") {
      obj.key = message.key;
    }
    if (message.comparator !== undefined && message.comparator !== 0) {
      obj.comparator = comparatorToJSON(message.comparator);
    }
    if (message.valueOneof?.$case === "value") {
      obj.value = message.valueOneof.value;
    } else if (message.valueOneof?.$case === "values") {
      obj.values = StringList.toJSON(message.valueOneof.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeafFilter>, I>>(base?: I): LeafFilter {
    return LeafFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeafFilter>, I>>(object: I): LeafFilter {
    const message = createBaseLeafFilter();
    message.key = object.key ?? "";
    message.comparator = object.comparator ?? 0;
    switch (object.valueOneof?.$case) {
      case "value": {
        if (object.valueOneof?.value !== undefined && object.valueOneof?.value !== null) {
          message.valueOneof = { $case: "value", value: object.valueOneof.value };
        }
        break;
      }
      case "values": {
        if (object.valueOneof?.value !== undefined && object.valueOneof?.value !== null) {
          message.valueOneof = { $case: "values", value: StringList.fromPartial(object.valueOneof.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseStringList(): StringList {
  return { values: [] };
}

export const StringList: MessageFns<StringList> = {
  encode(message: StringList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.values !== undefined && message.values.length !== 0) {
      for (const v of message.values) {
        writer.uint32(10).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StringList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStringList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.values!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StringList {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: StringList): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StringList>, I>>(base?: I): StringList {
    return StringList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StringList>, I>>(object: I): StringList {
    const message = createBaseStringList();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseOrderByStatement(): OrderByStatement {
  return { key: "", source: 0, direction: 0 };
}

export const OrderByStatement: MessageFns<OrderByStatement> = {
  encode(message: OrderByStatement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined && message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.source !== undefined && message.source !== 0) {
      writer.uint32(16).int32(message.source);
    }
    if (message.direction !== undefined && message.direction !== 0) {
      writer.uint32(24).int32(message.direction);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderByStatement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderByStatement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.direction = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderByStatement {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      source: isSet(object.source) ? dataSourceFromJSON(object.source) : 0,
      direction: isSet(object.direction) ? sortDirectionFromJSON(object.direction) : 0,
    };
  },

  toJSON(message: OrderByStatement): unknown {
    const obj: any = {};
    if (message.key !== undefined && message.key !== "") {
      obj.key = message.key;
    }
    if (message.source !== undefined && message.source !== 0) {
      obj.source = dataSourceToJSON(message.source);
    }
    if (message.direction !== undefined && message.direction !== 0) {
      obj.direction = sortDirectionToJSON(message.direction);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OrderByStatement>, I>>(base?: I): OrderByStatement {
    return OrderByStatement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OrderByStatement>, I>>(object: I): OrderByStatement {
    const message = createBaseOrderByStatement();
    message.key = object.key ?? "";
    message.source = object.source ?? 0;
    message.direction = object.direction ?? 0;
    return message;
  },
};

/**
 * Core is the central orchestration service that:
 * - Registers resources such as data functions, tasks and workflows
 * - Manages the lifecycle of triggering workflows
 * - Coordinates algorithm execution across distributed processors
 * - Tracks DAG execution state and handles distributed failure modes
 */
export type CoreService = typeof CoreService;
export const CoreService = {
  /**
   * Registers a worker, along with all assets defined in the worker's codebase.
   * This operation is idempotent on the worker name and git commit hash.
   */
  registerWorkerSnapshot: {
    path: "/Core/RegisterWorkerSnapshot" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterWorkerRequest): Buffer =>
      Buffer.from(RegisterWorkerRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterWorkerRequest => RegisterWorkerRequest.decode(value),
    responseSerialize: (value: RegisterWorkerResponse): Buffer =>
      Buffer.from(RegisterWorkerResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterWorkerResponse => RegisterWorkerResponse.decode(value),
  },
  /** Notify existence */
  registerServing: {
    path: "/Core/RegisterServing" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterServingRequest): Buffer =>
      Buffer.from(RegisterServingRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterServingRequest => RegisterServingRequest.decode(value),
    responseSerialize: (value: RegisterServingResponse): Buffer =>
      Buffer.from(RegisterServingResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterServingResponse => RegisterServingResponse.decode(value),
  },
  /** Triggers a workflow */
  triggerWorkflow: {
    path: "/Core/TriggerWorkflow" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: TriggerWorkflowRequest): Buffer =>
      Buffer.from(TriggerWorkflowRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): TriggerWorkflowRequest => TriggerWorkflowRequest.decode(value),
    responseSerialize: (value: TriggerWorkflowResponse): Buffer =>
      Buffer.from(TriggerWorkflowResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): TriggerWorkflowResponse => TriggerWorkflowResponse.decode(value),
  },
  /** Register the completion result of the data function */
  registerDataFunctionResult: {
    path: "/Core/RegisterDataFunctionResult" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterDataFunctionCompletionRequest): Buffer =>
      Buffer.from(RegisterDataFunctionCompletionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterDataFunctionCompletionRequest =>
      RegisterDataFunctionCompletionRequest.decode(value),
    responseSerialize: (value: RegisterDataFunctionCompletionResponse): Buffer =>
      Buffer.from(RegisterDataFunctionCompletionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterDataFunctionCompletionResponse =>
      RegisterDataFunctionCompletionResponse.decode(value),
  },
  /** Registers the result of a task */
  registerTaskResult: {
    path: "/Core/RegisterTaskResult" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterTaskResultRequest): Buffer =>
      Buffer.from(RegisterTaskResultRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterTaskResultRequest => RegisterTaskResultRequest.decode(value),
    responseSerialize: (value: RegisterTaskResultResponse): Buffer =>
      Buffer.from(RegisterTaskResultResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterTaskResultResponse => RegisterTaskResultResponse.decode(value),
  },
  /** Expose the internal state of the orchestration stack */
  exposeState: {
    path: "/Core/ExposeState" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ExposeStateRequest): Buffer => Buffer.from(ExposeStateRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExposeStateRequest => ExposeStateRequest.decode(value),
    responseSerialize: (value: ExposeStateResponse): Buffer => Buffer.from(ExposeStateResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExposeStateResponse => ExposeStateResponse.decode(value),
  },
  /** Query past results of tasks */
  queryTaskResult: {
    path: "/Core/QueryTaskResult" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: QueryParams): Buffer => Buffer.from(QueryParams.encode(value).finish()),
    requestDeserialize: (value: Buffer): QueryParams => QueryParams.decode(value),
    responseSerialize: (value: PastResults): Buffer => Buffer.from(PastResults.encode(value).finish()),
    responseDeserialize: (value: Buffer): PastResults => PastResults.decode(value),
  },
} as const;

export interface CoreServer extends UntypedServiceImplementation {
  /**
   * Registers a worker, along with all assets defined in the worker's codebase.
   * This operation is idempotent on the worker name and git commit hash.
   */
  registerWorkerSnapshot: handleUnaryCall<RegisterWorkerRequest, RegisterWorkerResponse>;
  /** Notify existence */
  registerServing: handleUnaryCall<RegisterServingRequest, RegisterServingResponse>;
  /** Triggers a workflow */
  triggerWorkflow: handleUnaryCall<TriggerWorkflowRequest, TriggerWorkflowResponse>;
  /** Register the completion result of the data function */
  registerDataFunctionResult: handleUnaryCall<
    RegisterDataFunctionCompletionRequest,
    RegisterDataFunctionCompletionResponse
  >;
  /** Registers the result of a task */
  registerTaskResult: handleUnaryCall<RegisterTaskResultRequest, RegisterTaskResultResponse>;
  /** Expose the internal state of the orchestration stack */
  exposeState: handleUnaryCall<ExposeStateRequest, ExposeStateResponse>;
  /** Query past results of tasks */
  queryTaskResult: handleServerStreamingCall<QueryParams, PastResults>;
}

export interface CoreClient extends Client {
  /**
   * Registers a worker, along with all assets defined in the worker's codebase.
   * This operation is idempotent on the worker name and git commit hash.
   */
  registerWorkerSnapshot(
    request: RegisterWorkerRequest,
    callback: (error: ServiceError | null, response: RegisterWorkerResponse) => void,
  ): ClientUnaryCall;
  registerWorkerSnapshot(
    request: RegisterWorkerRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterWorkerResponse) => void,
  ): ClientUnaryCall;
  registerWorkerSnapshot(
    request: RegisterWorkerRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterWorkerResponse) => void,
  ): ClientUnaryCall;
  /** Notify existence */
  registerServing(
    request: RegisterServingRequest,
    callback: (error: ServiceError | null, response: RegisterServingResponse) => void,
  ): ClientUnaryCall;
  registerServing(
    request: RegisterServingRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterServingResponse) => void,
  ): ClientUnaryCall;
  registerServing(
    request: RegisterServingRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterServingResponse) => void,
  ): ClientUnaryCall;
  /** Triggers a workflow */
  triggerWorkflow(
    request: TriggerWorkflowRequest,
    callback: (error: ServiceError | null, response: TriggerWorkflowResponse) => void,
  ): ClientUnaryCall;
  triggerWorkflow(
    request: TriggerWorkflowRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: TriggerWorkflowResponse) => void,
  ): ClientUnaryCall;
  triggerWorkflow(
    request: TriggerWorkflowRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: TriggerWorkflowResponse) => void,
  ): ClientUnaryCall;
  /** Register the completion result of the data function */
  registerDataFunctionResult(
    request: RegisterDataFunctionCompletionRequest,
    callback: (error: ServiceError | null, response: RegisterDataFunctionCompletionResponse) => void,
  ): ClientUnaryCall;
  registerDataFunctionResult(
    request: RegisterDataFunctionCompletionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterDataFunctionCompletionResponse) => void,
  ): ClientUnaryCall;
  registerDataFunctionResult(
    request: RegisterDataFunctionCompletionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterDataFunctionCompletionResponse) => void,
  ): ClientUnaryCall;
  /** Registers the result of a task */
  registerTaskResult(
    request: RegisterTaskResultRequest,
    callback: (error: ServiceError | null, response: RegisterTaskResultResponse) => void,
  ): ClientUnaryCall;
  registerTaskResult(
    request: RegisterTaskResultRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterTaskResultResponse) => void,
  ): ClientUnaryCall;
  registerTaskResult(
    request: RegisterTaskResultRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterTaskResultResponse) => void,
  ): ClientUnaryCall;
  /** Expose the internal state of the orchestration stack */
  exposeState(
    request: ExposeStateRequest,
    callback: (error: ServiceError | null, response: ExposeStateResponse) => void,
  ): ClientUnaryCall;
  exposeState(
    request: ExposeStateRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ExposeStateResponse) => void,
  ): ClientUnaryCall;
  exposeState(
    request: ExposeStateRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ExposeStateResponse) => void,
  ): ClientUnaryCall;
  /** Query past results of tasks */
  queryTaskResult(request: QueryParams, options?: Partial<CallOptions>): ClientReadableStream<PastResults>;
  queryTaskResult(
    request: QueryParams,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<PastResults>;
}

export const CoreClient = makeGenericClientConstructor(CoreService, "Core") as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): CoreClient;
  service: typeof CoreService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
